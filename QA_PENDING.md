# 🧾 QA Pending — in-game checks Deuce needs to run

_A running tally of changes that need a real in-game pass. **Pure-logic cores are unit-tested (`./gradlew test`) and trusted — they do NOT appear here.** This list is only the Minecraft-bound / behavior-changing stuff a headless test can't prove. `glow QA_PENDING.md`._

## How this works
- Every time I make an **MC/adapter/UI** change that needs verifying, it gets a row here.
- **Status:** ☐ pending · ✅ verified · ❌ failed (I note what broke + fix it).
- After you QA something, tell me the result and I'll move it to **Verified** (or fix it).
- **Deployed?** = is it in the live test jar yet. Items marked *not deployed* need a `./gradlew build` + copy first (I'll do that whenever you're ready to test).
- Older, broad in-game tests live in `TEST_CHECKLIST.md`; this file is the focused "did the recent changes actually work" tally.

### 🛠️ New QA/admin commands (this build)
- **`/gp augment set <function> <level>`** — the no-UI install path: hold a **Kernel** (breeding upgrade), run it to set an augment, then slot the Kernel into a pasture. Selectors use the level as an index — e.g. `/gp augment set nature 4` → Adamant, `/gp augment set ball 4` → master_ball; binaries `set ability 1` / `set egg_move 1`. `/gp augment list` shows the held Kernel; `set … 0` removes one; `/gp augment clear` strips all. **This is how you install Q31–Q34.**
- **`/gp goal set <species|any> <shiny> <minPerfectIvs> <count>`** — the breeding-goal tracker (Q37); `/gp goal` shows progress, `/gp goal clear` clears.

## ✅ QA Pass COMPLETE — 2026-06-30

> **The 2026-06-30 QA pass is closed.** Every row below is verified or trust-confirmed (the Daemon buff suite was
> validated end-to-end from the live log across all 4 mixin seams; the rest are trusted on Deuce's call). Issues
> found are tracked + being fixed in **`QA_RESULTS.md`** (BUG-001…006). A fresh QA session follows once the fixes land.

> 🚚 **DEPLOYED 2026-06-30** — jar md5 `558f8d5` (replaced `43679bc`) is live in *Greener Pastures Test*. **Every row below now ships in the running jar** (the old per-row "Deployed?" ❌ / "needs deploy" are superseded — the whole Q1–Q38 set is in this build). A **full MC restart** is required to load it; first check is a clean load (no mixin error).

| # | Change | Deployed? | How to verify | Status |
|---|--------|-----------|---------------|--------|
| Q1 | **Egg-queue**: breeding routes through a FIFO (buffers 5 tray + 24 queue, no silent discard past 5) | ❌ not yet | slot an upgrade, breed past 5 eggs → they keep coming; harvest the tray → it refills; relog → queue persists. `~/gp-logs/latest.log` shows `breeder brood`/`drain` | ✅ |
| Q2 | **ShinyOdds refactor** (`CobbreedingBridge` now delegates; behavior should be byte-identical) | ❌ not yet | breed with a shiny augment → `proc_shiny:true` still appears in `events.jsonl`; plain breeding unaffected (regression check) | ✅ |
| Q3 | **Daemon UI** rework (fit-viewport, scroll-zoom, **middle-drag=wire / side-drag=move**, top ports, text-stays-in-box, responsive header) | ✅ deployed | open Daemon: names stay inside boxes at any zoom; middle-drag wires, side-drag moves; PASTURE/KERNEL don't overlap | ✅ |
| Q4 | **BioBank Batch 1** (deposit/summary/persist/scatter) | ✅ deployed | `/give @s greenerpastures:biobank`, right-click eggs to bank (sneak=all), empty-hand=summary, relog persists, break scatters | ✅ |
| Q5 | **GpLog** observability pipe | ✅ deployed | `tail -F ~/gp-logs/latest.log` → `session_start` on load + `biobank.*` events when you use the BioBank | ✅ |
| Q6 | **Greener Kernel = 8 pairs** fills a 16-mon pasture | ✅ deployed | slot `breeding_upgrade_greener`, 16 mons → all 8 pairs breed | ✅ |
| Q7 | **Perf refactors** (per-dim registry, off-thread analytics, GUI caches — pure perf, behavior should be unchanged) | ❌ not yet | breeding still lays + queues + persists across relog; Arrange board still drags/drops/unpairs; highlighter still gold-glows shinies; Daemon still wires/pans/zooms; no red lines in the log | ✅ |
| Q8 | **Bug-hunt hardening batch** (see `BUG_HUNT.md`: breeder per-pasture crash-guard #1, pairings-packet bound/sanitize #2, container `isValid` guard #3, BioBank load-cap #4, log-writer reopen #9, menu slot-max #5, culler LRU #7, Daemon unpair cache, client-env gate #6 — all behavior should be **unchanged**, just safer) | ❌ not yet | normal breeding/deposit/highlight all still work; **shift-click a stack of Pasture Upgrades onto the wand → only 1 lands in the slot**, the rest stay in your bag; deposit eggs past 256 in a BioBank → it refuses, break scatters ≤256; `~/gp-logs/latest.log` shows no new errors (and a `breeder pasture_skip` line only if a pasture genuinely faults) | ✅ |
| Q9 | **Wave-2 fixes** (`BUG_HUNT.md` Wave 2: loot-sweep CUT, pasture-break recovery H1, phantom-record packet gate H2, log resilience H3/N2, idempotent analytics I1) | ❌ not yet | **loot no longer sweeps into a chest next to a pasture** (mechanic removed); **break a pasture that has an upgrade slotted + eggs queued → the upgrade item AND the eggs drop on the ground** (nothing lost), and `~/gp-logs/latest.log` shows `pasture break_cleanup`; no-wander sneak-toggle still works; breeding/pairing-save still work normally | ✅ |
| Q10 | **Dark-economy min-slice** (Renderer block culls non-keeper eggs → Data, per-player `DataStore`, Daemon item shows balance). Visuals are placeholder (crying-obsidian cube / echo-shard icon) — match BioBank's deferred-art state. | ❌ not yet | `/give @s greenerpastures:renderer` + `greenerpastures:daemon`; place the Renderer **touching a breeding pasture**; breed a while → **plain eggs disappear from the tray, ⭐SHINY + perfect-IV eggs STAY** (the sacred rule — verify hard); right-click the Daemon → chat shows **Data climbing** (10/egg); `~/gp-logs/latest.log` shows `renderer render` lines; **relog → Data persists**; break the Renderer → it drops itself | ✅ |
| Q11 | **Soul Tether item + multi-augment Kernel** (item tooltips off the tested logic; the Kernel `augments` component now holds the whole catalog, back-compat) | ❌ not yet | `/give @s greenerpastures:soul_tether` → "Blank — inscribe…"; hand-inscribe `…soul_tether[greenerpastures:tether={fn:"shiny",tier:2}]` → tooltip reads "Shiny Tether · Tier II · +20% · burns 16 Data/cycle (quality)"; a Kernel `breeding_upgrade_copper[greenerpastures:augments={shiny:30,speed:2}]` keeps **both** mods, and an **old single-shiny `{shiny:40}` save still loads** | ✅ |
| Q12 | **Soul Tether breeding loop** (slotted tether amplifies the base augment + drains Data; starved→free base; owner = tether-slotter) | ❌ not yet | Setup: a Renderer by the pasture (to earn Data), a Kernel with a Shiny augment **+** a Shiny Tether in a functional slot, then **open+close the wand** (records you as operator). **FED** (you have Data): shinies proc noticeably more + `~/gp-logs/latest.log` shows `tether drain` lines, balance falls. **STARVED** (Data at 0): proc drops back to base, **no drain**, breeding never pauses. A **Speed** augment + Speed Tether → visibly faster cadence. **Relog** → owner + multi-augments persist | ✅ |
| Q13 | **Tether swarm-fix edge cases** (`BUG_HUNT.md` Wave 3) | ❌ not yet | **Downgrade** a Kernel so a slotted tether's slot becomes hidden → that tether **stops amplifying + draining** (no charge for an unreachable slot); **pull all tethers** + reopen/close the wand → you **stop being charged** (owner cleared); a Kernel with a base **Enrichment** augment next to a Renderer → eggs render for **more Data** than the flat rate; sanity-check the balance — a trophy pasture's **own** renders should **not** out-earn its tether burn (you should need fuel pastures) | ✅ |
| Q14 | **Wave-4 robustness fixes** (`BUG_HUNT.md` Wave 4 — mostly invisible, just safer) | ❌ not yet | Renderer still culls + credits normally (no behavior change); **blow up / `/setblock` a pasture that has an upgrade + queued eggs → the items DROP + `~/gp-logs/latest.log` shows `pasture reclaim`** (not just on a player break); **disconnect with a Kernel+Augment sitting in a Compiler → those items drop in the world** (not lost); bulk-deposit a big egg stack into a BioBank → no lag hitch (decrypt cache); breeding/Renderer run for a while with **no new red lines** in the log | ✅ |
| Q15 | **Harvester block** (passive drops → its own chest, never on the ground; proc model) | ❌ not yet | `/give @s greenerpastures:harvester`; place it **touching a pasture** with tethered mons. Each mon has **3%/min** to proc a drop event → Cobblemon's faithful roll deposits into the block. It's a **slow trickle** (a full pasture ≈ 30/hr) — verify via `~/gp-logs/latest.log` `harvester harvest` lines rather than staring, or temporarily raise `BASE_PROC`. **Right-click opens a vanilla chest GUI** that fills over time, **nothing on the ground**; **break → contents drop**; fill it → **pauses** (no loss); **materials only**, no Data | ✅ |
| Q16 | **Kernel base +0.25% drop rate** (every Pasture Upgrade) | ❌ not yet | `/give @s greenerpastures:breeding_upgrade_copper` → hover shows **`⛏ +0.25% drop rate`**; a Harvester next to a pasture **with** a Kernel procs slightly more than one **without** (the +0.25% adds to the 3% base). Tune/verify the numbers with `python3 cobblemon-drops-ref/sim_drops.py` (no waiting) | ✅ **BUG-001 fixed + verified in-game** — ramps copper→greener |
| Q17 | **Drop Yield augment** (LEVER 2: widens Cobblemon's `amount` budget ceiling → richer events, base × tether) | ❌ not yet | `/give @s greenerpastures:breeding_upgrade_copper[greenerpastures:augments={drop_rate:25,drop_yield:2}]` → tooltip shows **`⛏ +2 drop yield`**; a Harvester next to that pasture keeps the **same cadence** but **richer events** — the rarer *secondary* drops climb (e.g. Froakie's `cobblemon:mystic_water` ~doubles) while the guaranteed lead drop (`pearlescent_froglight`) stays flat. Single-drop species barely change. Tune/verify headlessly with `python3 cobblemon-drops-ref/sim_drops.py --yield N` | ✅ |
| Q18 | **Tether-amplified drop rate/yield + Data drain** (the Harvester reads slotted Drop Rate/Yield tethers on ITS own clock; drains the operator's Data; starved → free base) | ❌ not yet | Setup: a Renderer by the pasture (earn Data), a Kernel with a **Drop Rate augment + a Drop Rate Tether** in a functional slot, then **open+close the wand** (claims you as operator). **FED** (you have Data): the Harvester procs noticeably more **and** `~/gp-logs/latest.log` shows `tether drain … "src":"harvester"` lines, balance falls. **STARVED** (Data at 0): proc drops back to base, **no drain**, harvesting never pauses (only a full chest pauses it). A **Drop Yield Tether** → richer events. **No double-charge:** a drop tether drains via the **Harvester**, the breeder's `tether drain` lines now only fire for **shiny/speed** (check the `src`). **Relog** → owner + tethers persist. Preview a fed tier-II rate tether with `sim_drops.py --droprate 0.30` (25×1.2) | ✅ |
| Q19 | **Tether-amplified Enrichment + Data drain** (the Renderer reads slotted Enrichment tethers on ITS own clock; amplifies the render-Data multiplier; drains the OPERATOR's Data; starved → base) | ❌ not yet | Setup: a Renderer touching a pasture, a Kernel with an **Enrichment augment + an Enrichment Tether** in a functional slot, **claim operator** (open/close wand). **FED** (operator has Data): non-keeper culls credit **more Data** than base **and** `~/gp-logs/latest.log` shows `tether drain … "src":"renderer"` lines (operator balance falls). **STARVED**: renders credit the **base** multiplier, **no drain**, culling never stops. The render **reward** still credits the Renderer's own owner — only the tether **burn** hits the operator. **No double-charge:** an enrichment tether drains via the **Renderer** only (the breeder's `tether drain` no longer fires for it). **Relog** persists | ✅ |
| Q20 | **Craftable augments** (Speed / Enrichment / Drop Rate / Drop Yield are now Compiler-installable, like Shiny — generic over function) | ❌ not yet | `/give @s greenerpastures:augment_drop_rate` (also `_speed`, `_enrichment`, `_drop_yield`, `_iv_floor`, `_ev`, plus the old `_shiny`) → tooltip shows the effect line. At a **Compiler**: slot a Kernel + the augment → the **preview** shows the merged mod; **Compile** consumes one augment and writes it onto the Kernel (e.g. Drop Rate → Kernel tooltip `⛏ +1.00% drop rate`, overwriting the base 0.25%; Drop Yield → `⛏ +1 drop yield`; Speed → faster cadence; Enrichment → richer renders). **Re-compiling the same augment is a no-op** (already installed; never downgrades). Items are **missing-texture** (art deferred) but fully functional — each then drives its live effect + tether amp (Q12/Q18/Q19/Q21). Inputs return on close/disconnect | ✅ |
| Q22 | **Rituals + type-drops LIVE** (Harvester reads the pasture's composition → config type-drops + gacha rituals; auto-pull interim) | ❌ not yet | First server boot writes `config/greenerpastures/rituals.json` (edit to retune / disable; `"enabled": false` kills it, staples unaffected). **Type-drop:** an **ice-type** in a Harvester'd pasture → `minecraft:ice` starts landing in the chest (`~/gp-logs/latest.log` `ritual drops`); a **fire-type** → `blaze_rod`. **Ritual:** compose **Fire + Dark + Ghost** mons → over time `netherite_scrap` drops (`ritual hit … nether_forge`). Ritual pulls accrue **per mon at ~1/3 the staple proc** (3× rarer — `rarityFactor`), so a fuller dedicated pasture pulls faster; pity guarantees the item eventually. To verify fast, edit the json (raise `baseChancePercent`, drop `hardPity`/`rarityFactor`). **Sableye + Fairy + Ghost** → `totem_of_undying`. **Augment scaling:** slot a **Drop Rate / Drop Yield** augment or tether on the Kernel → rituals visibly proc/yield MORE (same levers as staples). **Relog** → pity/banked-pull progress persists (Harvester NBT). Materials only, never Data | ✅ |
| Q21 | **IV Floor + EV breeding effects** (the breeder pre-shapes each bred egg's IVs/EVs; tethers amplify + drain) | ❌ not yet | Slot a Kernel `…augments={iv_floor:3,ev:20}` (or compile an **IV Floor / EV Augment**). Breed, then **HATCH** an egg → the hatchling has **≥3 perfect (31) IVs** and **20 EVs in every stat** (check its summary). IV Floor is a true floor (counts inherited 31s, **never lowers** a stat); EV set silently respects Cobblemon's 510 total. Add an **IV Floor / EV Tether** in a functional slot + **claim operator**: FED → the floor can round **up** (base 5 × tier III → 6 IVs) and `~/gp-logs/latest.log` shows the breeder `tether drain`; STARVED → base floor, no drain, breeding never pauses. Relog persists. (EV = flat per-stat floor; tune the magnitude/semantics if you want a targeted spread later) | ✅ **BUG-002 data+cmd fixed + verified** — per-stat EV alloc (`/gp augment ev`); alloc UI → web pass |
| Q23 | **Daemon "root" buffs — EFFECT + magnet slice** (held + fed Daemon grants rented buffs; tier = the Daemon's Mk level; Data drain = tier-scaled & summed). First boot writes `config/greenerpastures/buffs.json` (master + per-buff `enabled`/`maxTier`/`costPerSec`; `"enabled": false` kills it). **Live so far: Haste, Saturation, and the item Magnet** (enchant-boost + auto-smelt/vein-mine come next) — and you're **billed only for those three**, not the whole catalog. | ❌ not yet | `/give @s greenerpastures:daemon`; earn some Data (Renderer culls). **Hold** the Daemon (main or off hand) → within ~1s you gain **Haste + Saturation** (HUD icons, no particles) **and nearby dropped items fly toward you** (radius 4/6/8 by tier; freshly-tossed items wait out their pickup delay, so you can still drop things); `~/gp-logs/latest.log` shows `buff tick … paid:N`, balance ticks **down**; stop holding → effects lapse in ~3s, magnet + drain stop. **Tier:** in **creative**, sneak-right-click the Daemon to cycle **Mk I→II→III** (actionbar confirms) → Haste/Saturation amplifier + magnet radius rise with tier (Mk III = Haste III, 8-block pull) and per-sec drain scales ~3×. **Rented-while-fed:** spend Data to **0** → all buffs **stop** (no free-ride); earn more → they resume. **Survival** can't cycle level (Mk I only until the upgrade recipe ships). Relog: balance + Daemon level persist | ✅ |

| Q24 | **Daemon ENCHANT buff — Fortune (v1)** (held + fed Daemon adds its Mk tier to **Fortune** when you mine; "beyond vanilla max" — boosts an existing enchant, never grants from nothing). Delivered by two read-only mixins (`BlockDropBoostMixin` scopes a fed-holder window around `Block.getDroppedStacks`; `EnchantmentLevelMixin` adds the tier to `EnchantmentHelper.getLevel`) — **never writes the ItemStack, so there's no dupe/desync path by construction.** Billed like the others (FORTUNE in `SUPPORTED`). | ❌ not yet | Get a **Fortune III** pickaxe + a fed Daemon (Data > 0), hold the Daemon (off-hand works). Mine **diamond/redstone/lapis/coal ore** → drops should jump as if **Fortune III+tier** (Mk III = Fortune VI; mine a big batch and compare counts vs no-Daemon, or vs Data=0). `~/gp-logs/latest.log` shows `buff enchant_boost … ench:minecraft:fortune from:3 to:6`. **Negatives to confirm (the safety guarantees):** a pickaxe with **no Fortune** gets **nothing** (only rides an existing enchant); the boost does **NOT** show in the item **tooltip**, does **NOT** change **anvil/grindstone** combine costs, and does **NOT** apply to **other players** mining nearby; mine with Data at **0** → vanilla Fortune only. **No dupe:** the pickaxe's actual enchant NBT is unchanged (check in an anvil / `/data`), nothing left boosted after you stop. Drop to 0 Data mid-session → next ore is vanilla. (Looting + the value-effect enchants are v2 — see `ENCHANT_BOOST.md`.) | ✅ |

| Q25 | **Daemon HOOK buff — Auto-Smelt** (held + fed Daemon with auto_smelt → mined blocks drop **smelted**: raw_iron→iron_ingot, sand→glass, cobblestone→stone, etc.). Same read-only block-drop mixin window as Fortune — rewrites the loot list, **never touches your gear**; composes with Fortune (Fortune fattens the raw count, then it smelts). Binary (tier only sets cost, not effect). | ❌ not yet | Hold a fed Daemon, mine **iron/gold/copper ore** (or raw blocks, sand, cobblestone) → the **ingot/glass/stone** lands instead of the raw drop; `~/gp-logs/latest.log` shows `buff auto_smelt to:…`. **With Fortune too:** mine ore → multiple **ingots** (Fortune count × smelt). Blocks with no furnace recipe drop normally. Data at 0 or no Daemon → raw drops. Mobs/other players unaffected (block path only) | ✅ |

| Q26 | **Daemon HOOK buff — XP Boost** (held + fed Daemon with xp_boost → +25%/tier experience gain; Mk III = +75%). Read-only multiply on the incoming XP amount (`ServerPlayerEntity.addExperience`), no gear touched. | ❌ not yet | Hold a fed Daemon (xp_boost on), pick up **XP orbs** (mine ore, smelt, breed, etc.) → XP bar climbs faster than vanilla; `~/gp-logs/latest.log` shows `buff xp_boost from:N to:M tier:T`. Cycle Mk I→III → the multiplier grows (+25/50/75%). Data at 0 or no Daemon → vanilla XP | ✅ |

| Q27 | **Daemon HOOK buff — Vein-Miner** (held + fed Daemon with vein_mine → breaking an **ore or log** breaks the connected run of the same block at once; each drops proper loot, Fortune + auto-smelt apply per block). **Fenced** to ores (`c:ores`) + logs (`#logs`) only; same-block flood-fill; cap = tier×32 (Mk III = 96); needs the suitable tool; re-entrancy-guarded. Data rental is the cost (no tool durability used in v1). ⚠ **highest-risk buff — QA the safety first.** | ❌ not yet | **Safety first (do these before trusting it):** mine **dirt / stone / gravel** with a fed Daemon → **only the one block breaks** (NOT veinable, no cascade); break an ore with the **wrong/bare** hand → no vein. Then the feature: mine one block of an **iron/coal/diamond vein** → the whole vein pops, drops collect; chop one **log** of a tree → the trunk comes down. `~/gp-logs/latest.log` shows `buff vein_mine block:… broken:N`. **Composes on EVERY vein block (not just the first):** with Fortune → each vein ore multiplies; with auto-smelt → vein ores drop **ingots**; with a **Silk Touch** pickaxe → the whole vein drops as ore blocks (the real tool is used per block). **Bounds:** a huge ore field stops at the cap (~96), never runs away; creative/spectator never vein-mine; Data at 0 → single block only; one odd/modded block is skipped, never aborts the vein. (No block-XP from vein blocks + no tool damage in v1 — both tunable later) | ✅ |

| Q28 | **Daemon HOOK buff — Potion Duration+** (held + fed Daemon with potion_duration → **utility** effect durations +50%/tier; Mk III = +150%). **Allowlist only** (Night Vision, Water Breathing, Fire Resistance, Conduit Power, Dolphin's Grace, Slow Falling, Luck) — PvP-neutral, no combat effects ever extended. | ❌ not yet | Hold a fed Daemon (potion_duration on). Drink a **Night Vision / Water Breathing / Fire Resistance** potion → its timer is **longer** than vanilla (Mk III ≈ 2.5×); `~/gp-logs/latest.log` shows `buff potion_extend from:N to:M`. **PvP-neutral check:** a **Strength / Speed / Regen / Resistance** potion is **NOT** extended (vanilla duration). Data at 0 or no Daemon → vanilla durations. (Haste/Saturation aren't extended here — the Daemon grants those directly) | ✅ |

| Q29 | **Daemon ENCHANT buffs — attribute batch** (held + fed Daemon → **Respiration**, **Swift Sneak**, **Feather Falling** delivered as transient player **attribute modifiers** — no mixin, no ItemStack write, synced over the attribute channel). Tier = Daemon Mk level; granted **flat** (don't need the real enchant) and **stack on top** of real gear of the same kind (helmet Respiration + Daemon = beyond vanilla max). Modifiers are **temporary** (never saved) and stripped the moment the buff lapses. | ❌ not yet | Hold a fed Daemon (these on). **Respiration:** go underwater bare-headed → air drains **slower** (Mk III ≈ Resp III); with a Resp-III helmet → slower still. **Swift Sneak:** sneak → you move **faster crouched** than vanilla. **Feather Falling:** fall a few blocks → **less fall damage** (Mk III ≈ 45% less); `~/gp-logs/latest.log` shows `buff attr_apply buff:… tier:N value:…`. **Lapse check:** drop the Daemon / let Data hit 0 → effects revert to vanilla **immediately** (air drains normal, fall hurts full); relog mid-buff → no leftover modifier (check it's gone, re-applies if re-held). **Never heals:** big falls still deal damage (multiplier floored). **Not delivered (by design):** Efficiency (Haste already covers mining speed), Soul Speed (soul-block-gated). | ✅ |

| Q30 | **Daemon ENCHANT buffs — value-effect tier** (held + fed Daemon → **Lure**, **Luck of the Sea**, **Frost Walker**, **Looting** via entity-scoped read interception of `EnchantmentHelper` — no stack write). Lure = +5s/tier faster bites; LotS = +tier fishing luck; Frost Walker = +tier freeze radius; **Looting = +tier mob-loot** (Deuce opted in, **default-on** — the one combat-adjacent buff). All granted **from nothing** (no rod/boots/weapon needed); Fortune-style read-only safety. | ❌ not yet | Hold a fed Daemon (these on). **Lure:** fish → bites come **noticeably faster** than vanilla. **Luck of the Sea:** fish a while → **more treasure** over many casts (statistical; `~/gp-logs/latest.log` shows `buff fish_luck`/`buff fish_lure` at cast). **Frost Walker:** walk over water bare-booted → it **freezes under you** (radius grows with Mk); step off → melts. **Looting:** kill a mob with a fed Daemon (even bare-handed) → **more drops** than vanilla (Mk III ≈ Looting III); `buff equip_boost ench:…looting`. **Scope check:** stop holding / Data 0 → vanilla fishing, no freezing, vanilla mob loot. (Reminder: your server has no vanilla mobs, so Looting is for public servers — test on one that has mobs.) | ✅ |

| Q31 | **Breeding-meta augment — Nature lock** (the `NATURE` selector augment: its level = a 1-based index into the 25-nature `NatureCatalog`; the breeder writes `eggData.setNature(id)` before encrypt, exactly like IV/EV). Forces every egg from that pasture to a chosen nature, overriding vanilla/everstone inheritance. **Selector ⇒ never tether-amplified.** Effect + cores done; the *pick-which-of-25 UX* rides the deferred Compiler UI (set the `NATURE` level via creative/command for now). | ❌ not yet | On a managed pasture's Kernel, set the **NATURE** augment to index **4** (= Adamant per `NatureCatalog`) → breed → hatched eggs are **Adamant** regardless of parents/everstone; `~/gp-logs/latest.log` shows `breeding nature_lock nature:adamant`. Index **0** / no NATURE augment → **vanilla nature inheritance** (no lock). **Composes:** the same pasture's Shiny/IV-Floor/EV still apply to the egg. **Bad index** (e.g. 99) → no crash, just no lock. _(Install UX for the 25 natures = part of the breeding-meta Compiler-UI batch.)_ | ⏳ held — visual confirm needs inspection UI (BUG-005) |

| Q32 | **Breeding-meta augment — Ball lock** (the `BALL` selector augment: level = a 1-based index into `BallCatalog`'s 32 fully-namespaced ball ids; breeder writes `eggData.setPokeball("cobblemon:…")` before encrypt). Forces the egg's hatch ball, overriding vanilla "inherit mother's ball". Same selector/fail-safe shape as Nature. ⚠ **Verify the ball-id string format** — I used full ids (`cobblemon:poke_ball`); if Cobblemon's egg spec wants a bare path it fails *safe* (default ball, no crash), tell me and it's a 1-line catalog tweak. | ❌ not yet | Set the pasture's **BALL** augment to index **4** (= `master_ball`) → breed → eggs hatch in a **Master Ball**; `~/gp-logs/latest.log` shows `breeding ball_lock ball:cobblemon:master_ball`. Index **0** / none → vanilla mother's-ball inheritance. Try index **1** (poke_ball) + a gem ball (e.g. index 28 = azure_ball) to confirm the id format resolves. Bad index → no crash, default ball. Composes with Nature/Shiny/IV/EV on the same pasture. | ⏳ held — visual confirm needs inspection UI (BUG-005) |

| Q33 | **Breeding-meta augment — Hidden Ability** (the `ABILITY` augment, a **binary** toggle: any level = on). At egg build the bridge looks up the egg's species, scans its ability pool for the entry whose type isn't `CommonAbilityType` (= the hidden one) and writes `eggData.setAbility(name)`. Overrides vanilla ability rolling; fail-safe (species with no HA → no lock). ⚠ **Heuristic to confirm in-game:** "non-common type = hidden" — verify it actually lands the HA across a few species. | ❌ not yet | Enable the **ABILITY** augment on a pasture and breed a species with a well-known hidden ability (e.g. **Gible/Garchomp** line if present, or any mon whose HA differs from its normals). Hatch → the mon has its **hidden ability**, not a normal one; `~/gp-logs/latest.log` shows `breeding ability_lock ability:…`. **Off** / no augment → vanilla ability roll. A species with **no** HA → no lock, no crash (still hatches fine). Composes with Nature/Ball/Shiny/IV/EV. _(Install UX = the deferred Compiler UI; set via creative/command for now.)_ | ⏳ held — visual confirm needs inspection UI (BUG-005) |

| Q34 | **Breeding-meta augment — Egg Moves** (the `EGG_MOVE` augment, a **binary** toggle: any level = on). At egg build the bridge reads the egg species' `Learnset.getEggMoves()` and writes the first up-to-**4** onto the egg via `eggData.setMoves(...)` so the hatchling knows its egg moves (the hard-to-chain-breed ones). Overrides the default level-up moveset; fail-safe (no egg moves → untouched). Completes the breeding-meta batch (Nature/Ball/Ability/EggMoves). | ❌ not yet | Enable the **EGG_MOVE** augment and breed a species with notable egg moves → the hatchling **knows its egg moves** (check its moveset; capped at 4 slots); `~/gp-logs/latest.log` shows `breeding egg_moves moves:a,b,c,d`. **Off** / no augment → normal level-up moves. A species with **no** egg moves → unchanged, no crash. Composes with the other breeding augments. _(Verify the move-name token format resolves at hatch; fails safe if not.)_ | ⏳ held — visual confirm needs inspection UI (BUG-005) |

| Q35 | **Notifications — shiny ping** (F2). An observer on the analytics event stream (`Analytics.record` → `Notifier`): when a **shiny egg is laid**, ping the configured players. Pure `NotifyRules` decides; `config/greenerpastures/notifications.json` toggles it (master/`shiny`/`sound`/`channel` chat·actionbar·both/`target` all·ops). Wrapped so it can never break event recording. | ❌ not yet | With defaults (all players, chat + chime): breed until a **shiny egg is laid** → you get a chat line **"✨ Shiny egg laid at X, Y, Z!"** + a note-block chime; a **Daemon proc'd** shiny adds "— Daemon bonus proc!". **Config checks:** set `enabled:false` → silent; `sound:false` → message only; `channel:"actionbar"` → shows above hotbar; `target:"ops"` → only ops get it. Non-shiny eggs / other events → nothing. (Notifications file is created on first server start.) | ⏳ held — wants visual rework first |

| Q36 | **Notifications — Data milestone ping** (F2). On a Renderer credit (`egg_rendered`), if the owner's Data crossed into a new `dataMilestoneStep`-sized block (default 1000), ping **the owner only** (not broadcast). Stateless: `prev = balanceOf − gained`. Pure `NotifyRules.dataMilestone`; config `dataMilestone` (toggle) + `dataMilestoneStep`. | ❌ not yet | With defaults (step 1000): render eggs until your Data passes a thousand mark → **you** (the pasture/renderer owner) get **"💾 Data milestone: 1000!"** (then 2000, …) + chime; other players don't. A render that stays within the same block → no ping. Set `dataMilestone:false` → silent; `dataMilestoneStep:500` → pings every 500. Owner offline → no ping. | ⏳ held — wants visual rework first |

| Q37 | **Breeding Goal tracker — track-only** (F3, the mod's **first command**: `/gp goal`). Set a hunt (species/shiny/min-perfect-IVs/count); each egg a pasture **you operate** lays is folded into your progress; a "goal reached" ping fires when matches hit the count. **Non-destructive** — nothing is culled (auto-cull is the deferred follow-on). Per-session (in-memory; persistence is a follow-on). Pure cores `BreedingGoal`/`GoalProgress` are unit-tested; this row covers the command + observation + ping. | ❌ not yet | `/gp goal set gible true 4` → "🎯 goal set: shiny gible, ≥4 perfect IVs". Slot a Tether on a pasture so **you're its operator** (owner), breed → `/gp goal` shows **matched/checked** climbing + best IV total. When a **shiny gible with ≥4 perfect IVs** is laid → **"🎯 Breeding goal reached…"** chat + level-up sound (once). `/gp goal clear` → cleared; `/gp goal set any false 0 3` → any non-shiny ×3. **Non-destructive check:** off-target eggs are **NOT** removed (they still queue/collect normally). Goal resets on server restart (v1). `~/gp-logs/latest.log` shows `goal egg …` / `goal reached …`. | ⏳ held — wants visual rework first |

| Q38 | **Ghost Pasture — the real lag fix** (replaces the old broken no-wander). Sneak + empty-hand right-click a pasture → suppress it: tethered mons **stop existing as roaming entities** but their data keeps breeding + dropping loot. Method: a `@Redirect` blocks the one `World.spawnEntity` in Cobblemon's `tether()` for suppressed pastures (no new roamers), + a **one-time** despawn-on-toggle (`DISCARDED` then re-assert `tetheringId` → entity gone for good, tether intact). Flag persists on `PastureData`. **No per-tick work.** _(v1 = suppress direction; un-suppress re-materialising the discarded mons is increment 2.)_ | ❌ **needs deploy** | Tether a few mons to a pasture (they roam). **Sneak + empty-hand right-click** it → chat **"ghost pasture ON…"** and the **roamers vanish instantly**; F3 + entity count drops. **Data intact:** the pasture still **lays eggs** (breeding works) and the mons still show in its GUI — they were NOT untethered. **New tether while suppressed:** tether another mon → it does **not** spawn a roamer (data-only). **Persists:** save+quit, reload → still ghost, mons stay gone (don't reappear). ⚠️ **Watch for:** the mod loading without a mixin error (confirms the `@Redirect` resolved), and that suppressed mons aren't silently untethered (breeding must keep producing). Un-suppress (toggle OFF) currently just re-allows future spawns. | ✅ **BUG-003 fixed + verified** — un-hide re-materialises (rings the pasture, no dupes); breeding survives hide |

> When you're free for a QA pass: tell me and I'll `build` + deploy the latest (folds in Q1/Q2), then you run down the list.

## ✅ Verified — signed off by Deuce 2026-06-30

Deuce reviewed the board and **approved these as verified** (the detailed rows above stay put; this is the formal sign-off):
- **Recent bug fixes** — BUG-001 drop-rate ramp · BUG-002 EV per-stat · BUG-003 ghost-pasture un-hide · BUG-004 Daemon compile-your-own · BUG-006 breeding-compat _(detail in `QA_RESULTS.md`)._
- **Daemon buff suite (Q23–Q30)** — all 15 root buffs, end-to-end from the live log + the compile path.
- **Foundation + economy (Q1–Q22)** — egg queue/FIFO, ShinyOdds, BioBank, Renderer→Data, Soul Tethers, craftable augments, IV/EV floors, rituals + type-drops, Harvester.

**⏳ HELD — not signed off (revisit after the web-dev UI pass):**
- **Q31–Q34 breeding-meta locks** (Nature / Ball / Hidden Ability / Egg Moves) — code + logs fire (`breeding nature_lock` etc.), but visually confirming a hatchling carries the locked trait needs the **inspection UI** (node-graph per-mon nature/ability/moves view, **BUG-005**).
- **Q35–Q37 Notifications + Goal** (shiny-egg ping · Data-milestone ping · `/gp goal` tracker) — not tested yet; Deuce wants the **visual rework first**.

---

## 🆕 QA batch — 2026-07-03 · jar `f1769a7f` (deployed ✅)

> **What shipped:** #37 pasture health strip · #34 EV allocator · #35 nature/ball pickers (+ Ability/Egg-Move
> toggles in the Augmenter catalog) · Inbox `note_push`/`note_dismiss` log lines. Pure cores are unit-tested
> (14 new tests green: health matrix + picker-arg grammar) — the rows below are the MC/UI halves.
> **No GPU is burned by the Augmenter yet** (economy pass still deferred) — installs are slot-gated only.

| # | Change | How to verify | Status |
|---|--------|---------------|--------|
| Q39 | **Health strip — pasture view** | Notebook-open a pasture that's **unlinked + Kernel-less** → amber chips: `🔗 Not linked…` + `🧬 No Kernel…`. Click **link** → 🔗 chip clears live. Slot a Kernel → 🧬 clears; with **0–1 mons** tethered a `👥 Fewer than 2 parents` chip shows, gone at 2+. | ☐ |
| Q40 | **Health badges — Pastures tab** | Pastures tab: unhealthy pastures show **⚠N** on their list row (hover = the reasons); select one → the flags spell out in the detail pane. An **unloaded** pasture (e.g. the nether one while you're in the overworld) still badges link/Kernel/tray state — parent/bank checks silently skip (never guess an unloaded roster). | ☐ |
| Q41 | **BioBank-full flag** | Long-soak or force it: `/gp breed interval 15` on a Greener pasture full of Eevee×Ditto pairs → 256 banked Eevee in ~10 min → pasture view + Pastures tab grow `🏦 BioBank full for Eevee`; eggs start falling back to the tray (`egg_ingest full` in the log). | ☐ |
| Q42 | **Nature Lock picker** | Augmenter tab, Kernel in inventory → **Nature Lock → PICK…** → 25-nature grid with stat hints → pick **Adamant** → row chip shows `Adamant`; `/gp augment list` shows `Nature = 4 (adamant)`; slot the Kernel + breed → log shows `breeding nature_lock` and the egg card reads Adamant. | ✅ 2026-07-04 — grid pick + `nature_lock koffing bashful` ×3 in log + hatch confirm (Deuce) |
| Q43 | **Ball Lock picker** | Same flow → pick **Master Ball** (`/gp augment list` → `Poké Ball = 4 (…master_ball)`); bred hatchlings come out in it. | ✅ 2026-07-04 — moon-ball hatch confirmed (Deuce) |
| Q44 | **EV Primer allocator** | **EV Primer → PICK…** → sliders; try to exceed the budget → clamps at **510** (bar caps); set `0/252/0/0/6/252` → APPLY → chip `0/252/0/0/6/252`; `/gp augment list` shows the spread; bred egg cards carry those EVs. | ☐ |
| Q45 | **Ability Splice + Egg-Move Tutor rows** | Plain APPLY/REMOVE rows (no picker). APPLY both → `/gp augment list` shows `Hidden Ability = 1`, `Egg Moves = 1` → hatchlings get HA + egg moves (the Q31–Q34 backend, now UI-installable). | ✅ 2026-07-04 — HA hatch + egg-moves hatch both confirmed (Deuce) |
| Q46 | **Re-pick in place** | With Nature Lock installed → **EDIT** → pick Jolly → chip updates, **slots used unchanged**, log `augment_apply … repick:true`. REMOVE on EV Primer → chip gone + spread stripped (`/gp augment list`). | ☐ |
| Q47 | **Slot-capacity gate** | On a low-tier Kernel, fill every slot → remaining PICK…/APPLY buttons disable with *no free slots*. (Note: APPLY had a latent dead-button bug — the UI gated on a GPU cost the server never sent; fixed, so the whole tab is live now.) | ✅ 2026-07-04 — slot gate confirmed (Deuce) |
| Q48 | **Kernel LOADOUT chips** | Pasture view with the augmented Kernel slotted → a `LOADOUT` chip row under KERNEL: `🧬 Adamant · ◉ Master Ball · EV 0/252/0/0/6/252 · ✦ hidden ability · 📖 egg moves`. | ☐ |
| Q49 | **Inbox observability** | Catch-up → Inbox note → log shows `inbox note_push`; ✕ one → `note_dismiss id:N`; clear-all → `note_dismiss id:all n:K`. | ✅ 2026-07-04 — note_push/dismiss events in log |

---

## ⚡ Perf pass R3 — 2026-07-03 · jar `08a14db9` (NOT deployed yet — awaiting quit-confirm)

> **Everything below is behavior-preserving** — drop rates, catch-up counts, egg semantics byte-identical.
> 263 unit tests green (incl. the new GpProf suite). What changed: idle-off (no console work while it's closed),
> server-side change gating (identical payloads never re-sent), tick/save hoists, leak fixes, + the **built-in
> profiler** (`/gp perf`). Log level note: per-proc `harvest proc` lines now skip entirely when the log level
> is raised above DEBUG (default unchanged — you still see them).

| # | Change | How to verify | Status |
|---|--------|---------------|--------|
| Q50 | **Nothing regressed** (the big one) | Normal session: console opens instantly (incl. first-open after preload), pasture views swap seamlessly, breeding/harvest/catch-up all behave exactly as yesterday; no new red lines in the log. | ☐ |
| Q51 | **Idle-off** | Play 5 min WITHOUT opening the console → `gp-logs` shows NO `status_push` spam and no per-second push traffic; open the console → data is fresh within a tick (init sends a request + primes the bridge). Dev-browser workflow needs `-Dgreenerpastures.devbridge=true` now. | ✅ 2026-07-04 — 80+ min AFK, console closed: 7 status_push total (all pre-AFK), zero idle traffic |
| Q52 | **Change gate** | Console open on a quiet tab → log shows `status_push` only when Data/GPU actually change (was every second). Pull an item / bank an egg → the matching tab updates within ~1s as before. | ✅ 2026-07-04 — pushes only on real change while console open (7 total in 2h session) |
| Q53 | **`/gp perf`** | After a few minutes of play: `/gp perf` → chat table (breeder.scan / harvest.scan / net.push_batch / mcef.pump / bridge.serialize with counts + avg/max ms). `/gp perf flame` → writes `gp-logs/perf-flame.html`; open in a browser → nested flame graph, hover shows ms/counts. `/gp perf reset` starts a fresh window. | ✅ flame graph verified 2026-07-04 (no-flag session; mcef.pump 488ms/856s = 0.06%) — chat-table half w/ full sections next DEBUG session |
| Q54 | **Catch-up still exact + fast** | Nether hop 3+ min → return: `sweeps:N` exact + broods same-second (as Q-verified before), and with a BIG gap the return tick doesn't hitch (roster/pairs now hoisted out of the loops). | ✅ 2026-07-04 — sweeps:9 catch-up batch + gap_applied observed; exact, no hitch |
| Q55 | **Cross-dim cache keys** | Open a pasture in the overworld, then one at the SAME coords in the nether (if you have one) → each shows its own config, no bleed-through. | ☐ |

**For the mod listing (marketing):** screenshot `/gp perf flame` output + the table — "ships with its own
flame-graph profiler" is the optimization-as-a-feature story, and the numbers to quote are in PERF_AUDIT.md R3.

---

## 🚀 Release batch — 2026-07-03 evening · jar `c19c74b6` (NOT deployed — awaiting quit-confirm)

> ⚠️ **BEFORE YOU LAUNCH: add `-Dgreenerpastures.qa=true` to the instance's JVM args** (CurseForge →
> instance settings → Additional Java Arguments). Without it this build hides `/gp breed|harvest|data|daemon|augment`
> AND drops the log to INFO (release defaults). One flag restores both for the test instance.
> `/gp goal` + `/gp perf` always ship.

| # | Change | How to verify | Status |
|---|--------|---------------|--------|
| Q56 | **QA flag** | Launch WITHOUT the flag once: QA commands gone from tab-complete, log quiet (INFO), everything else works — that's what players get. Add the flag → commands + DEBUG log return (`session_start` shows `minLevel:DEBUG` + a QA MODE warn line). | ✅ no-flag half verified 2026-07-04 (commands absent, log INFO) — flag half on next relaunch |
| Q57 | **GPU costs — Augmenter** | With 0 GPU: every install button disabled/red; server also refuses (chat: *Not enough GPU*). Grab GPUs → install Shiny (2 ◈) and Speed (1 ◈) → GPU count drops; **EDIT re-pick costs nothing**; REMOVE refunds nothing. `/gp augment` (QA) still bypasses for testing. | ✅ 2026-07-04 — cost gate + GPU consumption + refusal confirmed (Deuce) |
| Q58 | **GPU costs — Compiler** | Buff + tier now consumes 2 ◈ per step (chat refusal when short); tier − is free, no refund. Costs show as ◈2 in the rows (was blank). | ☐ |
| Q59 | **Field Guide** | ~~New world / first join → a Field Guide lands in your inventory…~~ **DECIDED 2026-07-04: Field Guide item is being REMOVED** (redundant — it just opens the Notebook's Guide tab; Guide tab itself stays). Gift half already log-verified (`first_join_gift` fired 17:19). Open: what the first-join gift becomes (nothing / a Notebook / chat pointer). Row closes when the removal batch lands. | ✅ 2026-07-05 — removal batch landed (item/recipe/hook gone; Guide TAB stays; gift → Notebook, see Q87) |
| Q60 | **Creative tab** | Creative inventory → "Greener Pastures" tab (Notebook icon) with every mod item; Notebook + Guide also still in Tools. | ✅ 2026-07-04 |
| Q61 | **Listing collateral** | `glow LISTING.md` — read the store description; screenshot list marked 📸 is the shot plan for this QA session. CHANGELOG.md drafted. | ☐ |

---

## 🧪 Content batch — 2026-07-03 night · rituals, disks, recipes, snacks (NOT deployed)

| # | Change | How to verify | Status |
|---|--------|---------------|--------|
| Q62 | **Crafting recipes (13, rev #2 2026-07-04)** | notebook (copper+book+redstone+amethyst) · **blank disk NEW: 2 iron INGOTS + quartz BLOCK + redstone + paper** · **GPU NEW: 4 quartz BLOCKS corners + 2 redstone BLOCKS N/S + 2 iron BLOCKS E/W + kilobyte disk center** · soul tether (3 amethyst+echo shard+string) · daemon (echo+2 amethyst+GPU+megabyte disk) · **COPPER KERNEL NEW: 4 copper blocks corners + 4 quartz blocks cardinals + BLANK DISK center**; upgrades = kernel centered in 8 blocks of iron → gold → diamond; NETHERITE = 4 diamond blocks corners + 4 netherite ingots cardinals; GREENER = 4 netherite blocks corners + 4 emerald blocks cardinals. Greener drop +5.00% (sweep shows proc_pct 8.00). NB upgrading crafts a FRESH kernel — augment after upgrading. | ☐ |
| Q63 | **Data disks — write** | Dashboard → 💾 Disks card: buttons grey without a blank/balance; write a kilobyte (needs 1,024 Data + a blank) → blank converts, balance drops, log `disk write`. | ✅ 2026-07-04 — 3× `disk write` byte/8 in log |
| Q64 | **Data disks — read** | Right-click the written disk → +1,024 Data, you get the **blank back**; tooltips show values. Rocket disk = 67,108,864 (the flex). | ✅ 2026-07-04 — read-back verified; `disk read` DOES log at INFO (was in the rotated session file — nit withdrawn) |
| Q65 | **Type-drops** | Fill a linked pasture with Ice- or Fire-types → within a few sweeps ice/blaze rods appear in Harvester storage (log `harvest sweep` items). Fire-types are now your blaze-rod farm (no vanilla mobs!). Use `/gp harvest interval 5` to watch live. | ☐ |
| Q66 | **Gacha rituals** | Compose a pasture per a ritual in `config/greenerpastures/rituals.json` (defaults: e.g. Nether Forge) → each sweep banks a pull; hits log `ritual hit` + items deposit; **pity persists across relog** (check `pulls` DEBUG lines, pity climbing). | ✅ 2026-07-04 — proven via feast+black market (pulls bank per sweep, pity persists, hits deposit) |
| Q67 | **Ultra Compressed Snack (double-additive rev 2026-07-04)** | **Deuce's canonical test**: craft snacks [chilan+ega+starf] + [3 starf] + [3 starf] + [3 chilan] → grid → lore reads `×4 snacks · 3 effects · 11 stacks · 1 compressed away` = **4 chilan, 6 starf, 1 ega** (starf capped at 6 = double a pot cook). 9 identical shiny snacks → 6 shiny stacks max (27× exploit closed at 6×). Breadth still caps at 9 distinct effects; flavours sum but cap at 2× the strongest input. | ✅ 2026-07-04 — Deuce sign-off (canonical variants + caps verified vs decompile) |

---

## 🗡 Rituals v2 — hidden recipes (same night, jar `b8246aac`, NOT deployed)

> Rituals are now **hidden-achievement style**: compositions are secret until you first assemble one in a
> linked pasture — that moment it's LEARNED forever (per player), pops an Inbox note + a one-time chat line,
> and its full recipe renders in the new **Rituals tab**. Ritual rewards land in the tab's own **spoils pool**,
> never in Harvester storage. First hand-designed ritual ships: **Feast of the Blade**.

| # | Change | How to verify | Status |
|---|--------|---------------|--------|
| Q68 | **Hidden teaser** | Open Notebook → Rituals tab: mysterious empty state + "🔒 1 hidden ritual undiscovered". No recipe visible anywhere. | ✅ 2026-07-04 ('2 hidden', no hints) |
| Q69 | **Discovery pop** | In a linked pasture assemble **Kartana ×1 + Xerneas ×1 + Meowth ×8** (`/pokemon spawn` + tether, or `/spawnpokemon`). Next sweep (≤1 min, or `/gp harvest interval 5`): chat shows §6✦ Ritual discovered✦, Inbox gets the 🗡 note, tab now shows the full recipe card (8× Meowth · 1× Xerneas · 1× Kartana → 1× Enchanted Golden Apple). Log: `ritual discovered`. | ✅ 2026-07-04 — log `ritual discovered feast_of_the_blade` @ 42,69,12; no-flag session |
| Q70 | **The gacha itself** | Leave it running at `/gp harvest interval 5`: `ritual pulls` DEBUG lines show banked/pity climbing; ~2%/pull, soft pity 60, hard pity 120 → an apple within ~10 min at 5s sweeps. Hit → Inbox 🗡 note + apple appears in **Ritual spoils**, NOT in Storage. Lifetime hits count on the card. | ✅ 2026-07-04 — 2 hits in ~100 pulls @2%/soft-60, apples in spoils not Storage |
| Q71 | **Exactness** | Remove one Meowth (7 left) → pulls stop banking (composition broken); re-add → resumes with pity INTACT (persisted per pasture; also survives relog). Extra mons beyond the recipe don't break it (minimums). | ✅ 2026-07-04 — Xerneas pulled → pulls stopped; re-add resumed w/ pity intact (hits continued later in log) |
| Q72 | **Spoils pull + config regen** (REDESIGNED 2026-07-04 per Deuce: per-card spoils box) | Each ritual card now has its own spoils tile on the RIGHT (count + item; dimmed at 0) — pull the apples straight from the Feast card (L: one · ⇧: stack · R: all, inventory-full refusal). The bottom pool is now **Unclaimed spoils** — only items no learned card pays (e.g. breadcrumb Illicit Disks before Black Market is discovered); hidden when empty. Also: old pre-v2 config auto-regen (log `pre-v2 config detected`) — placeholder rituals gone, type-drops intact. | ☐ |
| Q73 | **Kernel egg-speed perk** | Every kernel now speeds breeding by its tier: copper ×1.1 · iron ×1.2 · gold ×1.3 · diamond ×1.4 · netherite ×1.5 · **greener ×1.6** (stacks with Speed augment ×1.5/×2/×3; 2.5-min floor still backstops). Verify: two identical pastures, copper vs greener kernel → greener broods visibly faster (`brood` log cadence ≈ base/1.6). QA override unaffected. | ☐ |
| Q74 | **Illicit Data Disk = corruption orb** | It's NOT currency anymore (gone from the Disks write panel; tooltip = contraband lore). Hold a Kernel + a disk → Augmenter shows **⛧ CORRUPT** → click: one of §aBLESSED§r (random augment beyond slot cap) / §dWILD§r (drop mod ×2 OR +1 pair — a 9-pair Greener!) / nothing / §cBRICKED§r (augments wiped OR tier −1), 30/25/25/20. Kernel is **corrupted forever**: ⛧ badge, all APPLY/EDIT/REMOVE locked, chat+Inbox reveal, log `corrupt kernel`. Corrupting again refuses. | ☐ |
| Q75 | **Black Market ritual** | Hidden ritual #2 (tab teaser now "2 hidden"). Assemble **Koffing ×4 + Ekans ×4 + Meowth ×1** in a linked pasture → discovery pop → gacha (2.5%, soft 50, hard 100) pays Illicit Data Disks into Ritual spoils. Existing v2 rituals.json auto-merges it (log `merged new hand-designed ritual`). | ✅ 2026-07-04 — discovered after linking (link gate was the blocker, by design); card + teaser verified |
| Q76 | **Renderer breadcrumb** | ~1/2000 voided eggs → Inbox whisper + an Illicit Data Disk appears in Ritual spoils (log `illicit_breadcrumb`). Long-soak — confirm opportunistically. | ☐ |
| Q77 | **Kernel hover tooltip — full loadout** (rev 2026-07-04: was only shiny/drop-rate/yield) | Hover an augmented Kernel in inventory: line 1 pairs·slots, then `N augments installed` + one line per augment — ✦ shiny %, ⚡ speed ×1.5/×2/×3, ▲ IV floor, EV hp/atk/def/spa/spd/spe (from the spread component), ◈ enrichment, ⛏ drop rate/yield, 🧬 Nature name, ◉ Ball name, ✦ hidden ability, 📖 egg moves. Corrupted kernel adds a dark-purple `⛧ corrupted — beyond modification` (+pair bonus if WILD). Deuce's moonball/3-IV kernel is the live test case. | ✅ 2026-07-04 — full-loadout hover confirmed (Deuce) |
| Q78 | **Ritual pulls counter** (QoL rev 2026-07-04) | Rituals tab, learned card header: `⟳ N pulls` next to lifetime hits, climbing ~1/sweep while composition holds (watch with `/gp harvest interval 5`); persists across relog; pre-existing saves start at 0; a hit does NOT reset it (lifetime, not since-last-hit). | ✅ 2026-07-04 — counters live (100 pulls feast / 4 black market), screenshot 683 |
| Q79 | **Masuda — two-player QA (PRE-PUBLISH GATE)** | Self-test impossible: `/givepokemon originaltrainer=…` gets overridden — the receiving player is always stamped OT. Plan: send the jar to a friend, they join, their mon (their OT) + Deuce's mon in one line → strip shows ✨ Masuda ✓, odds 1/2048 (config masuda 4.0); friend session doubles as fresh-eyes first-join/onboarding QA. One of the LAST steps before publishing. | 🗓️ deferred — 2-player session |
| Q80 | **Kernel/Daemon target selector** (backlog #5, rev 2026-07-04) | With 2+ Kernels in inventory → Augmenter grows a `Kernel target:` card row (#slot · tier · N augs · ⛧); click one → the whole tab (tier, slots, augments, corrupt) operates on THAT kernel; same for 2+ Daemons in the Compiler (`Daemon target:` cards, #slot · on/off · N buffs). Single item → no cards (unchanged). Move/drop the targeted item → tab falls back to first-found, never edits the wrong stack. Selection survives tab switches; resets on relog. | ☐ |
| Q81 | **Kernel rename** (QoL rev 2026-07-04) | Hold a Kernel, right-click (air) → small name screen (Enter saves, empty clears, 32-char cap) → name shows non-italic on the item tooltip, on its Q80 target card (replaces the tier label), and on the pasture KERNEL row (“name” · tier · pairs) when slotted. Log `kernel_rename`. Renaming does NOT touch augments. | ☐ |
| Q82 | **Professor's Summit — spanning ritual** (NEW 2026-07-04, rare-candy source) | Boot → log `merged new hand-designed ritual 'professors_summit'`, Rituals teaser +1 hidden. `/givepokemon` all 27 starters (Gens 1–9), tether ~14 in each of TWO linked pastures → next sweep of both: discovery pop, card shows `🏟 across 2 pastures` + 27 chips. Pulls bank 1/sweep (only one pasture of the pair banks — `ritual pulls professors_summit` from a single pos), 3%/soft 40/hard 80 → rare candies land on the card's spoils tile. Remove one starter → pulls stop, pity holds (it's on YOUR ledger — survives pasture demolition). One pasture alone (≤16 mons) must NEVER satisfy it. | ☐ |
| Q83 | **Snack Repel — charged-can design** (Snack Overdrive pt.1 REV 2, Deuce's cleaner spec 2026-07-04) | Craft cans: glass columns both sides + iron ingot top/bottom + nether wart center → 2× empty Snack Repel (grey how-to tooltip). **Charge**: can + 1–6 SAME-species typed berries in any grid → charged can, red lore `🚫 ÷N Xxx Types` (1 chilan = ÷10 Normal, 6 = ÷60; 7+ berries still ÷60; mixed species / already-charged can / non-typed berry (starf) → no recipe). **Bake**: charged can(s) + snacks in the ultra craft → snack lore carries the ÷N lines; EMPTY can in the ultra grid → recipe refuses. 3 same-type cans cap at 2× strongest (÷120). Place snack → repelled type's spawns collapse; attracts unaffected; survives block break + chunk reload; ambient spawns untouched. | ☐ |
| Q84 | **Snack spawn-speed credit queue** (Snack Overdrive pt.2, NEW 2026-07-05) | Vanilla rot replaced: EVERY bite-time copy now counts multiplicatively (6 EGA = ×0.9⁶ interval; 6 golden apples = ×0.178 ≈ 5.6× throughput — plain gold beats enchanted, Cobblemon's own data). Ultra snack lore gains a truthful `⚡ spawn speed ×N.N` gold line (the Cobblemon 'Reduce Bite Time' line still lies — ignore it). Verify: two snacks, plain vs 6-golden-apple → count spawns ~15 min, heavy one ≈5× more; log `snack speed_tick mult/spawns/banked` per release; walk away → credits bank (cap 8), return → burst-capped release (≤3/tick); credits survive chunk reload. No bite seasonings = exactly vanilla cadence. | ☐ |
| Q85 | **Echo/amethyst progression drops** (NEW 2026-07-05) | Boot → log `merged 5 new default type-drop(s)`, `rituals.json` gains ghost/dark→echo_shard (3%/2%) + fairy/psychic/rock→amethyst_shard (8%/5%/5%) — existing tuned entries UNTOUCHED. Fill a linked pasture with ghost-types → echo shards trickle into Harvester storage (~29/hr full mono at default sweep; `/gp harvest interval 5` to watch); fairy/psychic/rock pastures → amethyst. Tether + Daemon are now craftable without ancient-city loot. Echo stays the rarest entry in the table. | ☐ |
| Q86 | **Specimen Disks — mon compression v1** (NEW 2026-07-05; placeholder green disk art until Deuce's sprite) | Craft: blank data disk + amethyst shard + echo shard (shapeless) → blank Specimen Disk (grey tooltip). Notebook → **Specimens tab**: live party list + blank count; ARCHIVE → mon leaves party, written disk lands in the blank's slot (tooltip `✨ Species · Lv.N · gender`, gold when shiny; log `specimen compress`). **Rails** (each refuses with its own chat line): mid-battle ✗ · last party member ✗ · no blank ✗ · full inventory ✗. Right-click written disk → mon back to party (PC when full), **blank returned**, log `specimen release`. **Lossless check**: archive a shiny with known IVs/nature/moves → release → EVERYTHING identical. Relog with mon on disk → still releases fine. | ☐ |
| Q87 | **Field Guide removed — Notebook is the first-join gift** (2026-07-05) | Existing world: any held Field Guides vanish (item unregistered — expected, pre-release world only); no crash on load; creative tab + Tools show Notebook, no Guide item. Fresh player (or wipe `greenerpastures_greeted` from world data): first join → a **Notebook** lands in inventory, log `first_join_gift`; right-click air → console opens; **Guide tab still in the tab bar** with full content. Notebook recipe unchanged (crafting a second one still works). | ☐ |
| Q88 | **Ritual Batch 2 — 14 hinted rituals** (2026-07-05, spec in RITUALS.md) | Boot → log `merged new hand-designed ritual` ×14; Rituals tab shows **🔒 locked cards with italic hints** (Nether Star … Sniffer Egg) + '2 hidden' for the unhinted OGs (feast/BM learned already → 1: summit). Spot-checks: **Saddle** (1 Mudsdale+2 Zebstrika+4 Ponyta, LOW ≈3 hits/hr) discovery + card; **Shulker** 8 Shuckle → 2 shells/hit; **Elytra** any-mix gate: 6 Heracross ✓, 3+3 ✓, 5 beetles ✗ (chip reads `6× Heracross / Pinsir`); **Ominous Bottle** demands REAL Alolan Meowth/Persian + Galarian Zigzagoon (Kanto strays don't count — chips read `2× Alolan Meowth` etc.); **Music Disc**: hits roll different discs into the card's spoils tile (`🎲 a random music disc` chip); **Wither chain**: skull ritual trickles while star ritual crawls (0.4%, hard pity 400). Fossil communion = 11 fossils across 2 pastures (span chip). | ☐ |
| Q89 | **Pokémon-egg cake** (community request, 2026-07-05) | Vanilla cake shape (3 milk / sugar-EGG-sugar / 3 wheat) but the egg slot accepts ANY Cobbreeding Pokémon egg (`#c:eggs` tag — all ~300 type variants) → vanilla cake; buckets return empty. Vanilla chicken-egg cake still works. Yes, you can bake a shiny egg. That's on you. | ☐ |
| Q90 | **MissingNo. — the 1M capstone** (NEW 2026-07-05) | Dashboard grows a glitchy ▓▒░ M̸i̷s̶s̴i̵n̷g̸N̵o̶. ░▒▓ card: lifetime-rendered odometer + progress bar to the next million + SUMMON (grey until owed). QA: `/gp data` does NOT move the odometer (only rendered eggs do — void some meowths, watch `lifetimeEarned` climb in the dashboard channel); to test the summon quickly, void eggs after temporarily... it's 1M — QA via a direct DataStore poke is fine or trust the math tests. Summon → party gains **MissingNo.** (nicknamed, never shiny): species GLITCHES every ~5s among Ditto/Aerodactyl/Gastly/Marowak/Kabutops, never same twice (log `missingno rotate`); send it out → visibly rotates in-world. **Battles refuse** while it's in the party (chat: 'distorts the battlefield'); box/Specimen-Disk it → battles work again; release from disk → still glitches. Party+PC full → refusal WITHOUT burning the claim. One summon per million, forever (`SUMMON (2)` if 2M unclaimed). | ☐ |
| Q91 | **Hatch Haste augment** (NEW 2026-07-05 — closes the hatching bottleneck) | Augmenter grows a 🐣 **Hatch Haste** row (throughput, 1◈, 1 slot): APPLY level I → newly bred eggs' hover timer reads **half** the usual (log `breeding hatch_haste from/to`); `/gp augment set hatch 2|3` → ×0.25 / ×0.1 (1s floor — never instant); eggs laid BEFORE install keep their old timer; kernel tooltip + `/gp augment list` show `🐣 hatch ×0.5`; a Hatch tether amplifies it like any throughput mod; composes with the whole breeding-meta loadout. Timer still drains at Cobbreeding's pace (incubator ability still doubles drain). | ☐ |
| Q92 | **Augment level II** (Deuce's design 2026-07-05: 1.5× effect, 3 slots total) | Augmenter: installed magnitude augments grow an **UPGRADE** button (same ◈ fee again) → row shows `II` badge, slots jump 1→3 (pips), effect 1.5×: shiny 45%, drop +3.00%, yield +2, enrichment +30%, IV floor 4, speed ×2, hatch ×0.25. **Copper kernel (2 slots) can NEVER host a level II** (refusal chat: 'needs 3 slots total'); Greener maxes at 2× II + 2× I. Selectors/EV/binaries have no upgrade. REMOVE frees all 3 slots, no refund. Corrupted kernels can't upgrade. **Also fixed in this batch**: bare Greener no longer phantom-occupies a slot with its born drop-rate base (and can now actually INSTALL Drop Rate — verify: fresh Greener shows 0/8 used, Drop Rate row installable, tooltip shows +5.00%→+7.00% after install); BLESSED corruption gift is base-aware. Old kernels re-read correctly (level detection is base-relative). | ☐ |
| Q93 | **Adversarial round 1+2 - criticals & majors** (2026-07-05, REVIEW_FINDINGS.md) | Spot-checks: **sacred rule**: hand-corrupt an egg read (or trust tests) - unreadable eggs route KEEP; **Tier III**: corrupt kernels until BLESSED on an installed augment → `⛧III` badge (2× base: shiny 60%, IV floor 5 max), Augmenter NEVER offers III; **WILD on Greener** → an installed augment jumps a tier (chat says 'pushed to tier N'), non-Greener still gets +1 pair; **MissingNo**: tether attempt refused ('refuses confinement'), never appears in pair lists; **pause exploit dead**: toggle breeding off 10 min (loaded) → on → NO burst; **catch-up billing**: low Data + tethers + nether-hop → log `tether drain ... starvedMidway:true`, later eggs base-rate; **span clique**: 3 overlapping fossil pastures → exactly ONE `ritual pulls fossil_communion` line per sweep; **repel survives recompress**: ultra a repel snack again → 🚫 line intact. | ☐ |
| Q94 | **Adversarial UX batch** (2026-07-05) | Fresh eyes: first-ever console open lands on **Guide** (later opens: BioBank); first-join chat names the Notebook; Pastures empty state says Notebook (no 'wand'); Guide has **Key recipes** + **Snack Science** sections, no Field Guide mentions; adding the FIRST parent to a line auto-creates + wires a **→ BioBank** sink; pasture full → **Inbox ⚠ note** (once); learned ritual cards show **⏳ pity N/hard**; inventory overlay header reads **[E]**; Missingno card hidden until Data rendered; summon says party vs PC truthfully; GPU/Illicit never taken from offhand; 7 berries + can = no recipe; written disks warn about death; BRICKED degrade mentions augment loss; battle refusal visible to BOTH PvP sides. | ☐ |
